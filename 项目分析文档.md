
# 预约服务小程序 - 项目分析文档

## 1. 项目概述

### 1.1 项目简介
这是一个基于 **Taro 框架** 开发的预约服务小程序，主要面向美业、健身、餐饮等服务型商家，提供线上预约解决方案。项目包含**小程序端**（C端用户预约）和**管理后台**（B端商家管理）两部分。

### 1.2 核心价值
- **C端用户**：随时随地预约，无需电话沟通，三步完成预约
- **B端商家**：降低人工成本，减少漏单，提升客户体验

---

## 2. 技术架构

### 2.1 技术栈概览

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **前端框架** | Taro + React | 跨端小程序 + H5 |
| **样式** | Tailwind CSS 4 + weapp-tailwindcss | 原子化样式，跨端适配 |
| **UI组件库** | shadcn/ui (Taro版) | `@/components/ui/*` |
| **图标库** | lucide-react-taro | Taro 适配版 Lucide 图标 |
| **状态管理** | zustand | 轻量级状态管理 |
| **后端框架** | NestJS | RESTful API |
| **ORM** | Drizzle ORM | 轻量 TypeScript ORM |
| **数据库** | SQLite (better-sqlite3) | 零配置文件级存储 |
| **认证** | JWT + Passport | 管理后台身份验证 |
| **密码加密** | bcryptjs | 安全哈希存储 |

### 2.2 技术架构图
```
┌─────────────────────────────────────────────────────────────────┐
│                          前端层 (Taro + React)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐ │
│  │ 小程序端页面 │  │ 管理后台页面 │  │ UI组件库 (shadcn/ui)  │ │
│  └──────────────┘  └──────────────┘  └────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 网络请求封装 + zustand状态管理                             │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓ HTTP
┌─────────────────────────────────────────────────────────────────┐
│                         后端层 (NestJS)                         │
│  ┌──────────────────┐  ┌─────────────────────────────────────┐ │
│  │   控制器层       │  │     服务层 (Service)                 │ │
│  │ (Controller)     │  │  - appointment.service.ts            │ │
│  │  - service       │  │  - service.service.ts                │ │
│  │  - appointment   │  │  - auth.service.ts                   │ │
│  │  - auth          │  │  - time-slot.service.ts              │ │
│  │  - user          │  │  - user.service.ts                   │ │
│  └──────────────────┘  └─────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   守卫与装饰器                              │ │
│  │  - JwtAuthGuard (JWT认证)                                  │ │
│  │  - RolesGuard (角色授权)                                   │ │
│  │  - @Public() (公开接口)                                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓ Drizzle ORM
┌─────────────────────────────────────────────────────────────────┐
│                       数据库层 (SQLite)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │ services     │  │appointments  │  │  categories      │     │
│  └──────────────┘  └──────────────┘  └──────────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │ time_slots   │  │    users     │  │                  │     │
│  └──────────────┘  └──────────────┘  └──────────────────┘     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 项目结构分析

### 3.1 目录结构

```
/workspace/
├── src/                          # 前端源码
│   ├── pages/
│   │   ├── index/                # 首页（服务展示）
│   │   ├── appointments/         # 预约记录
│   │   ├── profile/              # 我的
│   │   ├── service-detail/       # 服务详情
│   │   ├── booking/              # 预约提交
│   │   ├── booking-success/      # 预约成功
│   │   └── admin/                # 管理后台
│   │       ├── login/            #   登录
│   │       ├── dashboard/        #   仪表盘
│   │       ├── services/         #   服务管理
│   │       ├── appointments/     #   预约管理
│   │       └── users/            #   用户管理
│   ├── components/ui/            # UI 组件库（shadcn/ui）
│   ├── hooks/
│   │   └── use-auth-guard.ts     # 管理后台登录校验 Hook
│   ├── network.ts                # 网络请求封装
│   ├── app.config.ts             # 应用配置（页面路由 + TabBar）
│   ├── app.css                   # 全局样式 + Design Token
│   └── app.tsx                   # 应用入口
│
├── server/                       # 后端源码
│   └── src/
│       ├── db/
│       │   ├── schema.ts         # 数据库表定义
│       │   ├── index.ts          # 数据库初始化 + 种子执行
│       │   └── seed.ts           # 种子数据
│       ├── modules/
│       │   ├── service/          # 服务管理模块
│       │   ├── appointment/      # 预约管理模块
│       │   ├── time-slot/        # 时段查询模块
│       │   ├── auth/             # 认证模块（JWT + Passport）
│       │   └── user/             # 用户管理模块
│       ├── common/
│       │   ├── guards/           # JWT Guard + Roles Guard
│       │   ├── decorators/       # @Public() + @Roles()
│       │   └── utils/            # 统一响应格式
│       └── main.ts               # 后端入口
│
├── .cozeproj/prototype/mobile/   # 移动端原型（HTML）
├── docs/                         # 文档目录
├── PRD.md                        # 产品需求文档
├── BACKEND_DESIGN.md             # 后端技术方案
├── DESIGN.md                     # 设计风格文档
├── README.md                     # 项目文档
└── package.json                  # 项目依赖配置
```

### 3.2 核心文件说明

| 文件/目录 | 作用 |
|----------|------|
| [server/src/db/schema.ts](file:///workspace/server/src/db/schema.ts) | 数据库表定义（categories, services, appointments, time_slots, users） |
| [server/src/main.ts](file:///workspace/server/src/main.ts) | NestJS 后端启动入口，配置 CORS、全局拦截器 |
| [src/app.tsx](file:///workspace/src/app.tsx) | Taro 前端应用入口，配置 Lucide 图标和 Toast |
| [src/app.config.ts](file:///workspace/src/app.config.ts) | 页面路由、TabBar 配置 |
| [src/network.ts](file:///workspace/src/network.ts) | 网络请求封装 |

---

## 4. 核心功能模块

### 4.1 小程序端（C端）

#### 4.1.1 首页
- 功能：分类筛选 + 服务卡片列表 + 搜索
- 技术要点：响应式布局 + Tailwind CSS
- 路径：`/pages/index/index`

#### 4.1.2 预约记录
- 功能：Tab 状态筛选 + 预约列表 + 取消预约
- 路径：`/pages/appointments/index`

#### 4.1.3 我的
- 功能：用户信息 + 预约统计 + 编辑昵称/电话 + 管理后台入口
- 路径：`/pages/profile/index`

#### 4.1.4 预约流程
1. 服务详情页（`/pages/service-detail/index`）
2. 预约提交页（`/pages/booking/index`）：选日期 + 选时段 + 联系信息 + 确认提交
3. 预约成功页（`/pages/booking-success/index`）

### 4.2 管理后台（B端）

#### 4.2.1 认证模块
- 登录页面（`/pages/admin/login/index`）
- JWT + 角色认证

#### 4.2.2 仪表盘
- 预约统计 + 修改密码
- 路径：`/pages/admin/dashboard/index`

#### 4.2.3 服务管理
- 新增/编辑/删除服务项目
- 路径：`/pages/admin/services/index`

#### 4.2.4 预约管理
- 查看预约列表 + 修改预约状态
- 路径：`/pages/admin/appointments/index`

#### 4.2.5 用户管理
- 新增/删除管理员用户
- 路径：`/pages/admin/users/index`

---

## 5. 数据库设计

### 5.1 核心数据表

#### categories（分类表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT | 分类名称 |
| key | TEXT | 分类标识（唯一） |
| icon | TEXT | 图标名称（可选） |
| sort_order | INTEGER | 排序权重 |
| status | TEXT | 状态（active/inactive） |
| created_at | TEXT | 创建时间 |

#### services（服务项目表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT | 服务名称 |
| description | TEXT | 服务描述 |
| price | INTEGER | 价格（分） |
| duration | INTEGER | 时长（分钟） |
| image_url | TEXT | 封面图 URL |
| category_id | INTEGER FK | 关联分类 |
| sort_order | INTEGER | 排序权重 |
| status | TEXT | 状态（active/inactive） |
| created_at | TEXT | 创建时间 |

#### appointments（预约记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| service_id | INTEGER FK | 关联服务 |
| service_name | TEXT | 服务名称（冗余） |
| service_price | INTEGER | 服务价格（冗余） |
| appointment_date | TEXT | 预约日期（YYYY-MM-DD） |
| time_slot | TEXT | 预约时段（HH:mm） |
| contact_name | TEXT | 联系人姓名 |
| contact_phone | TEXT | 联系人电话 |
| status | TEXT | 状态（pending/completed/cancelled） |
| created_at | TEXT | 创建时间 |

#### users（管理用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | TEXT UNIQUE | 用户名 |
| password | TEXT | bcrypt 加密密码 |
| role | TEXT | 角色（admin/user） |
| must_change_password | BOOLEAN | 是否需要修改密码 |
| created_at | TEXT | 创建时间 |

---

## 6. API 接口设计

### 6.1 公开接口（小程序端，无需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/services` | 服务列表（支持 `category` 筛选） |
| GET | `/api/services/:id` | 服务详情 |
| GET | `/api/time-slots` | 可用时段查询（`serviceId` + `date`） |
| POST | `/api/appointments` | 创建预约 |
| GET | `/api/appointments` | 预约列表（支持 `status` 筛选） |
| PUT | `/api/appointments/:id/cancel` | 取消预约 |

### 6.2 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录（返回 JWT + mustChangePassword） |
| GET | `/api/auth/profile` | 获取当前用户信息 |
| PUT | `/api/auth/change-password` | 修改密码 |

### 6.3 管理接口（需 JWT + admin 角色）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/users` | 用户列表 |
| POST | `/api/users` | 新增用户 |
| DELETE | `/api/users/:id` | 删除用户 |
| POST | `/api/services` | 新增服务 |
| PUT | `/api/services/:id` | 更新服务 |
| DELETE | `/api/services/:id` | 删除服务 |
| GET | `/api/appointments/admin/list` | 管理端预约列表 |
| PUT | `/api/appointments/admin/:id/status` | 修改预约状态 |

---

## 7. 安全机制

1. **JWT 认证**：管理后台所有接口需携带 Bearer Token
2. **全局 Guard**：JwtAuthGuard 全局生效，小程序端接口 @Public() 放行
3. **角色守卫**：@Roles('admin') 限制管理接口仅管理员可访问
4. **密码加密**：bcryptjs 哈希存储，永不存明文
5. **默认密码**：admin 首次登录 mustChangePassword=true，强制修改
6. **登录校验**：前端 useAuthGuard Hook，未登录自动跳转登录页

---

## 8. 开发与部署

### 8.1 项目脚本

| 命令 | 说明 |
|------|------|
| `pnpm install` | 安装依赖 |
| `coze dev` | 启动开发环境（前端5000 + 后端3000） |
| `pnpm validate` | 校验代码 |
| `pnpm build` | 编译构建 |

### 8.2 默认账号

| 系统 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理后台 | admin | admin | 首次登录强制修改密码 |

---

## 9. 设计风格

### 9.1 配色方案

| 用途 | 色值 | Tailwind 类名 |
|------|------|---------------|
| 主色（橙） | #F97316 | `bg-orange-500` |
| 主色深 | #EA580C | `bg-orange-600` |
| 主色浅 | #FFF7ED | `bg-orange-50` |
| 强调色 | #FB923C | `text-orange-400` |
| 背景色 | #FFFFFF | `bg-white` |
| 页面底色 | #F9FAFB | `bg-gray-50` |
| 正文色 | #111827 | `text-gray-900` |
| 副文色 | #6B7280 | `text-gray-500` |

### 9.2 视觉风格
- 现代简约，温暖友好
- 圆角卡片布局，视觉层次分明
- 清晰的间距和色彩系统

---

## 10. 项目亮点总结

1. **现代化技术栈**：Taro + React + Tailwind CSS + NestJS + Drizzle ORM
2. **前后端分离**：清晰的架构设计，RESTful API 接口
3. **完整的认证体系**：JWT + 角色权限控制
4. **跨端支持**：支持微信小程序、抖音小程序、H5 多端
5. **完善的 UI 组件库**：shadcn/ui 提供丰富的组件
6. **安全设计**：bcrypt 密码加密、强制首次修改密码
7. **模块化设计**：良好的代码组织结构

---

## 11. 后续优化建议

1. **性能优化**：添加接口缓存、图片懒加载
2. **数据同步**：考虑添加 Redis 缓存热点数据
3. **消息通知**：集成微信消息通知功能
4. **多商家支持**：支持多商家入驻和切换
5. **数据统计**：增加更详细的商家数据统计和分析
6. **单元测试**：添加完整的单元测试和集成测试
