# 预约服务小程序 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品定位
为美业、健身、餐饮等服务型商家提供线上预约解决方案的小程序。解决传统电话预约效率低、易漏单的痛点，让用户可以随时在线浏览服务项目并完成预约。

### 1.2 目标用户
- **C 端用户**：需要预约美容、美发、健身、餐饮等服务的消费者
- **B 端商家**：提供预约制服务的中小型商户（本次 MVP 聚焦 C 端体验）

### 1.3 核心价值
- 用户：随时随地预约，无需电话沟通，三步完成
- 商家：降低人工成本，减少漏单，提升客户体验

---

## 2. 功能模块

### 2.1 服务展示模块（首页）

#### 2.1.1 服务分类
- 顶部展示服务分类标签（如：全部 / 美业 / 健身 / 餐饮）
- 点击分类标签筛选对应服务

#### 2.1.2 服务列表
- 卡片式展示服务项目，每张卡片包含：
  - 服务封面图
  - 服务名称
  - 服务简述（一行描述）
  - 服务价格（突出显示）
  - 预约按钮
- 列表支持下拉刷新

#### 2.1.3 服务详情
- 点击服务卡片进入详情页，展示：
  - 服务封面大图
  - 服务名称
  - 服务价格
  - 服务详细描述（多段落文本）
  - 服务时长
  - 底部固定「立即预约」按钮

### 2.2 在线预约模块

#### 2.2.1 预约流程（三步完成）
**Step 1 - 选择服务**：从服务详情页点击「立即预约」进入预约页面，服务信息自动带入

**Step 2 - 选择时间**：
- 日期选择：展示近 7 天可选日期，横向滑动选择
- 时段选择：展示当日可用时段（如 09:00、10:00、11:00...），已约满的时段置灰不可选

**Step 3 - 确认预约**：
- 展示预约摘要：服务名称 + 日期 + 时段
- 填写联系人信息：姓名、手机号
- 点击「确认预约」提交

#### 2.2.2 预约结果
- 提交成功后展示成功页面
- 显示预约详情（服务、时间、联系人）
- 提供「查看我的预约」和「返回首页」两个操作

### 2.3 个人中心模块

#### 2.3.1 用户信息
- 头像 + 昵称展示
- 预约统计（总预约数、待服务数）

#### 2.3.2 预约记录
- Tab 切换：全部 / 待服务 / 已完成 / 已取消
- 每条预约记录卡片包含：
  - 服务名称
  - 预约日期 + 时段
  - 服务价格
  - 预约状态标签（待服务/已完成/已取消）
- 待服务状态的预约支持「取消预约」操作

---

## 3. 页面结构

```
TabBar（3 个 Tab）
├── 首页（服务展示）
│   ├── 分类筛选栏
│   └── 服务列表
├── 预约（我的预约）
│   └── 预约记录列表
└── 我的
    ├── 用户信息区
    └── 预约统计

非 TabBar 页面
├── 服务详情页
├── 预约提交页（选择时间 + 确认）
└── 预约成功页
```

---

## 4. 页面交互流程

```
首页 → 点击服务卡片 → 服务详情页 → 点击「立即预约」
  → 预约提交页（选日期/时段 + 填信息）→ 确认预约
  → 预约成功页 → 返回首页 / 查看我的预约

预约Tab → 预约记录列表 → 取消预约（仅待服务状态）

我的Tab → 用户信息 + 预约统计
```

---

## 5. UI 设计规范

### 5.1 设计风格
- 现代简约，温暖友好
- 橙色为主色调，传达活力与信赖感
- 圆角卡片布局，视觉层次分明

### 5.2 配色方案
| 用途 | 色值 | Tailwind 类名 |
|------|------|---------------|
| 主色（橙） | #F97316 | `bg-orange-500` |
| 主色深 | #EA580C | `bg-orange-600` |
| 主色浅 | #FFF7ED | `bg-orange-50` |
| 强调色 | #FB923C | `text-orange-400` |
| 背景色 | #FFFFFF | `bg-white` |
| 页面底色 | #F9FAFB | `bg-gray-50` |
| 正文色 | #111827 | `text-gray-900` |
| 副文色 | #6B7280 | `text-gray-500` |
| 分割线 | #E5E7EB | `border-gray-200` |
| 成功色 | #22C55E | `text-green-500` |
| 警告色 | #EF4444 | `text-red-500` |

### 5.3 间距系统
- 页面边距：`px-4`
- 卡片内边距：`p-4`
- 列表项间距：`gap-3`
- 模块间距：`gap-6`

### 5.4 圆角规范
- 卡片圆角：`rounded-2xl`
- 按钮圆角：`rounded-xl`
- 标签圆角：`rounded-full`

---

## 6. 数据模型

### 6.1 服务项目 (Service)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| name | string | 服务名称 |
| description | string | 服务描述 |
| price | number | 价格（分） |
| duration | number | 服务时长（分钟） |
| image_url | string | 封面图 URL |
| category | string | 分类（beauty/fitness/food） |
| status | string | 状态（active/inactive） |
| created_at | date | 创建时间 |

### 6.2 预约记录 (Appointment)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| service_id | number | 关联服务 ID |
| service_name | string | 服务名称（冗余） |
| service_price | number | 服务价格（冗余） |
| appointment_date | string | 预约日期 (YYYY-MM-DD) |
| time_slot | string | 预约时段 (HH:mm) |
| contact_name | string | 联系人姓名 |
| contact_phone | string | 联系人电话 |
| status | string | 状态（pending/completed/cancelled） |
| created_at | date | 创建时间 |

### 6.3 时段配置 (TimeSlot)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 主键 |
| service_id | number | 关联服务 ID |
| date | string | 日期 (YYYY-MM-DD) |
| start_time | string | 开始时间 (HH:mm) |
| max_capacity | number | 最大容量 |
| booked_count | number | 已预约数 |

---

## 7. API 接口设计

### 7.1 服务相关
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/services | 获取服务列表（支持 category 筛选） |
| GET | /api/services/:id | 获取服务详情 |

### 7.2 预约相关
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/appointments | 获取预约列表（支持 status 筛选） |
| POST | /api/appointments | 创建预约 |
| PUT | /api/appointments/:id/cancel | 取消预约 |

### 7.3 时段相关
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/time-slots | 获取指定服务+日期的可用时段 |

---

## 8. 非功能需求

### 8.1 性能
- 首页加载 < 2s
- 预约提交响应 < 1s

### 8.2 跨端适配
- 微信小程序 + H5 双端支持
- 适配主流手机机型（iPhone SE ~ iPhone 15 Pro Max）

### 8.3 容错
- 网络异常时展示重试提示
- 预约提交失败保留用户填写的数据

### 8.4 安全
- 手机号等敏感信息传输加密
- 防重复提交

---

## 9. MVP 范围

### 本期实现（P0）
- 服务列表 + 分类筛选
- 服务详情
- 三步预约流程
- 预约记录查看 + 取消
- 个人中心基础展示
- 后端数据持久化（SQLite）

### 后期迭代（P1-P2）
- 用户登录注册
- 商家管理后台
- 通知提醒（预约成功/即将到时）
- 评价系统
- 多商家切换
